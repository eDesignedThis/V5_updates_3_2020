#invitation-form
    .form-group
        label(for='loginEmail') Invitation ID
        .form-control
            input#email.loginusername(name='email' type='text' placeholder='' style='' required='1' label='Username' maxlength='75')

    .form-group
        button.btn.btn-primary.login-button(type='submit' value='' name='')
            input#cmdSubmit.loginbutton(type='submit' name='cmdSubmit' value='Submit ' title='Click to login' onclick='return check(this.form);')
    #error_message.error